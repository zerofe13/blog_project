import React from 'react';
import styled from 'styled-components';

const AuthFromBlock = styled.div``;

const AuthFrom = () => {
  return <AuthFromBlock>Authform</AuthFromBlock>;
};

export default AuthFrom;
